import { ReactNode } from 'react';
interface AppLayoutProps {
  children: ReactNode;
}
declare const AppLayout: ({
  children,
}: AppLayoutProps) => import('react/jsx-runtime').JSX.Element;
export default AppLayout;
